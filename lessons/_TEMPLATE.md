# <tag> — <title>

> **Pillar:** <loop | toolkit | context | governance | swarm>
> **Adds:** `<component>`
> **Builds on:** `<previous tag>`

## The idea

One paragraph: what capability this lesson adds and *why an agent needs it*.

## What changed vs. the previous tag

```bash
git diff <previous-tag>..<this-tag> --stat
```

- Bullet the key additions (new function, new schema entry, new file).

## Walkthrough

Explain the core code for this component — the function, its safety checks, and
how it's wired into `TOOLS` / `TOOL_SCHEMAS` (or the loop).

## Try it

```bash
git checkout <tag>
python coding_agent.py
```

## Takeaways

- 2–3 bullets a reader should remember.
